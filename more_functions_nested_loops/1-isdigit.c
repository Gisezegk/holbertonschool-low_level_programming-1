#include "main.h"
#include <stdio.h>

/**
 * _isdigit - print upper
 *
 * Return: 1 if Upper
 *
 * @c: character v
 */

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
	return (1);
}
else
{
	return (0);
}
}

