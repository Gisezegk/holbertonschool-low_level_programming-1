#include "main.h"

/**
 * _strchr - epico
 * @c: var
 * @c: tac
 * Return: always var
 *
 */


char *_strchr(char *s, char c)
{
	int cont = 0;

	while (cont[s])
	{
		cont++;
	
	if (cont[s] == c)
	{
		return(s);
	}
	else
	{
	return ('\0');
	}
	
	}
	return(0);
}
